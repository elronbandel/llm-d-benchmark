# [REQUIRED]
{% set base_dir = base_dir | default('../') -%}
base_dir: {{ base_dir }}

# [REQUIRED]
values_file:
  path: {{ base_dir }}/config/templates/values/defaults.yaml

# [REQUIRED]
template_dir:
  path: {{ base_dir }}/config/templates/jinja

# [OPTIONAL]
scenario_file:
  path: {{ base_dir }}/config/scenarios/guides/fma-autoscaling.yaml

# FMA + WVA: Fast Model Actuation deployment with WVA scaling the requester
# ReplicaSet. Companion to workload-autoscaling.yaml (modelservice + WVA);
# same workload shape, different deployment mechanism.
